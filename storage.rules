rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permettre l'accès aux fichiers pour les utilisateurs authentifiés
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
